#!/bin/bash
set -eo pipefail

if [[ -n "${CLAUDE_CODE_ENABLE:-}" ]]; then
  curl -fsSL https://claude.ai/install.sh | bash

  if [[ -n "${CLAUDE_CODE_OAUTH_REFRESH_TOKEN:-}" ]] && [[ -z "${ANTHROPIC_API_KEY:-}" ]]; then
    claude auth login
  fi
fi
