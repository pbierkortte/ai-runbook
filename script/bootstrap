#!/bin/bash
set -euo pipefail
DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"

[[ -n "${ENABLE_DOTFILES:-}" ]] || exit 0

"$DIR/install-yq"
"$DIR/install-skills"
"$DIR/rebuild-manifest"
"$DIR/install-cline"
"$DIR/install-claude"
"$DIR/link-skills"
"$DIR/stage-protocols"
"$DIR/hydrate-agents"
"$DIR/configure-git"
source "$DIR/configure-python"
"$DIR/write-dotfilesrc"
"$DIR/wire-shell"
"$DIR/refresh-docs"
