#!/bin/bash
set -euo pipefail
DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"

[[ -n "${ENABLE_DOTFILES:-}" ]] || exit 0

source "$DIR/install-yq"
source "$DIR/install-skills"
source "$DIR/rebuild-manifest"
source "$DIR/install-cline"
source "$DIR/install-claude"
source "$DIR/link-skills"
source "$DIR/stage-rules"
source "$DIR/hydrate-agents"
source "$DIR/configure-git"
source "$DIR/configure-python"
source "$DIR/write-dotfilesrc"
source "$DIR/wire-shell"
source "$DIR/refresh-docs"
