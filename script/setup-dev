#!/bin/bash
# setup-dev
# Sets up an AI-friendly dev environment.
# Installs tools, configures shells, and wires up skill dirs.
#
# Usage: bash script/setup-dev

set -euo pipefail

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

source "$DIR/install-deps"
source "$DIR/env-vars"
bash "$DIR/stage-protocols"
git config filter.agents.clean 'printf "<!-- DO NOT EDIT THIS FILE IS AUTOMATICALLY GENERATED -->\n"'
git config filter.agents.smudge cat
bash "$DIR/hydrate-agents"
source "$DIR/shell-hooks"
source "$DIR/setup-skills"
source "$DIR/openskills-manifest"
source "$DIR/install-agents"
