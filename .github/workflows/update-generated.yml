name: Update Generated Files

on:
  push:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Hydrate Agents
        run: bash script/hydrate-agents
      - name: Update Contents
        run: bash script/update-contents
      - name: Update Manifest
        run: bash script/update-manifest
      - name: Setup Dev
        run: bash script/setup-dev
      - name: Dehydrate Agents
        run: bash script/dehydrate-agents
      - name: Commit changes
        run: bash script/commit-changes
